# soma
sum = 3 + 4

# sub
sub = 5 - 2

# multiplicação
mult = 5 * 5

# divisão

div1 = 5 / 2  # divisão padrão que retorna um float
div2 = 4 / 2  # divisão padrão que retorna um float
div3 = div(5,2)  # divisão que retorna um inteiro truncado
div4 = div(4,2)  # divisão que retorna um inteiro

# resto de divisão
res = rem(5,2)

IO.puts(sum)
IO.puts(sub)
IO.puts(mult)
IO.puts(div1)
IO.puts(div2)
IO.puts(div3)
IO.puts(div4)
IO.puts(res)
